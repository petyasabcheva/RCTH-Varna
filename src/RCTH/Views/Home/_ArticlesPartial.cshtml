@{
    this.ViewData["Title"] = "All Blog Articles ";
}

<div>
    @foreach (var article in this.Model.ArticleList.Articles)
    {
        <div >
            <h3>
                @article.Title
            </h3>
            <p>@article.ShortContent</p>
            <p>@article.AuthorName</p>

            <a asp-action="ById" asp-route-id="@article.Id">Details</a>
        </div>
    }
</div>

